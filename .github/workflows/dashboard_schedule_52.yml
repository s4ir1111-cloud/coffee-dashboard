name: Dashboard Scheduler 52

on:
  schedule:
    - cron: '52 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    uses: ./.github/workflows/update_dashboard.yml
    secrets: inherit

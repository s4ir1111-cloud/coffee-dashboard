name: Dashboard Scheduler 22

on:
  schedule:
    - cron: '22 * * * *'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  update:
    uses: ./.github/workflows/update_dashboard.yml
    secrets: inherit
